# 服务端口
PORT=3000

# 日志级别
LOG_LEVEL=info

# 数据库路径（相对于 server/）
DB_PATH=./data/outlook.db

# 数据库提供者：node 运行时默认 sqlite，Cloudflare Worker 运行时默认 d1
DB_PROVIDER=sqlite

# 当 DB_PROVIDER=d1 时使用的 D1 绑定名
D1_DATABASE_BINDING=DB

# 访问密码（留空则不启用认证）
ACCESS_PASSWORD=
